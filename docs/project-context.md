# Project Context

本项目是一个 Chrome 插件，用于辅助用户检查 X 关注账户活跃度。

MVP 采用本地页面扫描方式，不使用 X 官方 API，不接云端服务器。

核心边界：
1. 不自动取关。
2. 不自动关注。
3. 不读取 Cookie、Token、密码、私信。
4. 不上传任何数据。
5. 不绕过 X 限制。
6. 不高速自动访问页面。
7. 用户手动打开页面，插件只读取当前可见页面内容。

Codex 每次开发前必须阅读本文件。
